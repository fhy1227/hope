---
description: Godot 4.7 C# 开发规范与项目约定
globs: "**/*.cs"
alwaysApply: false
---

# Godot 4.7 C# 开发规范

> 场景树、目录、ProcessMode 完整契约见 **`docs/ARCHITECTURE.md`**（修改架构前必读）。

## 脚本结构

- 所有挂载到节点的脚本必须声明 `public partial class X : NodeBaseType`
- 节点实例化用 `PackedScene.Instantiate<T>()`，禁止 `new NodeType()`

## 节点引用与 Export

- 跨场景访问优先 `Combat.Instance?.World` / `Combat.Instance?.Run`，禁止 `GetParent().GetParent()`

## 场景树契约（`scenes/gameplay/combat.tscn`）

```
Combat (Combat.cs, Always)
├── GameWorld (Pausable) → Levels / Entities / Enemies / Effects / Pickups / RunManager
├── UI_Hud / UI_Overlay / UI_Pause / UI_Transition / UI_Debug
```

## 目录约定

| 层 | 路径 | 用途 |
|----|------|------|
| gameplay | `scripts/gameplay/` | 战斗玩法、实体、组件、战斗 UI |
| meta | `scripts/meta/` | 主菜单、背包/装备管理器 |
| framework | `scripts/framework/` | Autoload、配置、存档、ScenePaths |

- 命名空间保持既有约定（`Hope.Core`、`Hope.Systems`、`Hope.UI` 等），与物理目录可不完全一致
- 场景路径集中定义在 `scripts/framework/core/ScenePaths.cs`
- 跨模块通信优先走 `EventBus`

## 职责边界

| 类型 | 可做 | 不可做 |
|------|------|--------|
| `Combat` | 引用世界/UI；重置战斗临时状态 | 玩家移动、攻击逻辑 |
| `RunManager` | 波次循环、生成玩家 | 直接操作 UI 节点 |
| UI 脚本 | 展示、输入、订阅信号 | 直接改敌人列表 |
