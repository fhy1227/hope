---
description: Hope 项目架构契约（AI 必读）
alwaysApply: true
---

# Hope 架构契约

修改场景结构、新增 UI 层、Autoload、或跨模块引用前，**必须先阅读** `docs/ARCHITECTURE.md`（完整契约）。专题见 `docs/存档方案.md`、`docs/技能系统-实现总结.md`。

## 三层目录

| 层 | scenes | scripts | 职责 |
|----|--------|---------|------|
| **gameplay** | `scenes/gameplay/` | `scripts/gameplay/` | 战斗对局：关卡、实体、波次、战斗 UI、技能施放 |
| **meta** | `scenes/meta/` | `scripts/meta/` | 局外：主菜单、Hub、结算、角色级 Manager |
| **framework** | — | `scripts/framework/` | 基础设施：Autoload、配置、存档、Steam、`ScenePaths` |

```
MainMenu (meta) → Hub (meta) → Combat (gameplay) → Settlement (meta) → Hub
```

## 战斗场景树（`combat.tscn`）

```
Combat (Combat.cs, Always)
├── GameWorld (Pausable)     ← Levels / Entities / Enemies / Effects / Pickups / RunManager
├── SkillSystems             ← 冷却 / 怒气 / 施放（战斗临时，不存档）
├── UI_Hud (1) / UI_Overlay (2) / UI_Pause (3) / UI_Transition (4) / UI_Debug (10)
```

层级常量：`scripts/gameplay/types/UiLayers.cs`。新战斗 UI **必须**挂到上表对应层，勿随意新增无名 `CanvasLayer`。

## 硬性规则

1. 游戏入口 `scenes/meta/main_menu.tscn`；战斗根 `scenes/gameplay/combat.tscn` + `scripts/gameplay/Combat.cs`（`Combat.Instance`）
2. 场景路径写入 `ScenePaths`；切换场景禁止硬编码 `res://scenes/` 字符串
3. 跨模块访问：`Combat.Instance`（战斗内）→ Autoload → `EventBus`；**禁止** `GetParent().GetParent()`
4. 实体用 `PackedScene.Instantiate<T>()`，禁止 `new Enemy()` 等
5. 关卡不拥有玩家；`RunManager.SpawnPlayer()` 生成到 `Entities`，位置取自 `BaseLevel.SpawnPoint`
6. 角色级数据（背包、装备、技能树、副本进度）在 `scripts/meta/systems/`，由 `PersistenceMgr` 局外存档；**禁止**进战斗时 `Clear()` 参与者。战斗临时状态（冷却、怒气等）仅在 `Combat.ResetCombatState()` 清理
7. 配置改 `tools/config/*.xlsx` 重新导出，禁止手改 `scripts/framework/config/*Config.cs`
8. 新增 Autoload：`PersistenceMgr` 必须排在所有 `[PersistedData]` 参与者之后
9. `RunManager` 不操作 UI 节点；`Player` 不 `ChangeScene`

## Autoload 与持久化

| Autoload | 层 | 存档 |
|----------|-----|:----:|
| ConfigBootstrap, EventBus, GameManager, AudioManager, SteamManager | framework | — |
| InventoryManager, EquipManager, SkillManager, DungeonManager | meta | ✓ |
| SkillDB | gameplay* | — |
| PersistenceMgr | framework | 读写 |

\* `SkillDB` 脚本在 `gameplay/skill/`，全局只读；加点数据在 `SkillManager`（meta）。

`[PersistedData]` 参与者：`InventoryManager`、`EquipManager`、`SkillManager`、`DungeonManager`。

## 状态枚举（勿混用）

| 枚举 | 维护者 | 含义 |
|------|--------|------|
| `GameState` | `GameManager` | 应用级：Menu / Hub / Combat / Settlement |
| `RunPhase` | `RunManager` | 单局：Combat / FateCard / Shop / GameOver / Victory |
| `CombatState` | `RunManager` | 战斗中 / 暂停 / 结束 |

## 战斗 UI 速查

| 层 | 典型面板 | 快捷键 |
|----|----------|--------|
| UI_Hud | GameHud、SkillBar | — |
| UI_Overlay | Shop、Inventory、FateCard、SkillTree | `I` 背包 · `K` 技能树 |
| UI_Pause | PauseMenu、DeathDialog | `Esc` 暂停 |

## 新增功能检查清单

- [ ] 代码在 `gameplay` / `meta` / `framework` 对应目录？
- [ ] 场景路径已加入 `ScenePaths`？
- [ ] 战斗 UI 挂在 `combat.tscn` 既定 `CanvasLayer`（`UiLayers`）？
- [ ] 跨模块走 `EventBus` 或 `Combat.Instance` / Autoload？
- [ ] 角色级数据经 `[PersistedData]` + `PersistenceMgr`？
- [ ] 战斗临时状态仅在 `Combat.ResetCombatState()` 清理？
- [ ] 实体 `PackedScene.Instantiate<T>()`？
- [ ] 配置改 xlsx，未手改 `*Config.cs`？

## 目录速查

`gameplay/` 战斗 · `meta/` 局外 · `framework/` 基础设施
