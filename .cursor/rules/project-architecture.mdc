---
description: Hope 项目架构契约（AI 必读）
alwaysApply: true
---

# Hope 架构契约

修改场景结构、新增 UI 层、Autoload、或跨模块引用前，**必须先阅读** `docs/ARCHITECTURE.md`。

## 三层目录

| 层 | scenes | scripts | 职责 |
|----|--------|---------|------|
| **gameplay** | `scenes/gameplay/` | `scripts/gameplay/` | 战斗对局：关卡、实体、波次、战斗 UI |
| **meta** | `scenes/meta/` | `scripts/meta/` | 局外：主菜单、设置、背包/装备管理器 |
| **framework** | — | `scripts/framework/` | 基础设施：Autoload、配置、存档、Steam |

## 硬性规则

1. 战斗对局根场景为 `scenes/gameplay/combat.tscn`，协调脚本 `scripts/gameplay/Combat.cs`（`Combat.Instance`）；游戏入口为 `scenes/meta/main_menu.tscn`
2. 新战斗 UI 挂到 `combat.tscn` 既定 `CanvasLayer`（`UiLayers.Hud/Overlay/Pause/Transition/Debug`）
3. 跨模块用 `EventBus` 或 `Combat.Instance`，禁止 `GetParent().GetParent()`
4. 实体用 `PackedScene.Instantiate<T>()`，禁止 `new Enemy()` 等
5. 关卡不拥有玩家；玩家由 `RunManager` 生成到 `Entities`，位置取自 `BaseLevel.SpawnPoint`
6. 角色级数据（背包、装备）在 `scripts/meta/systems/`，由 `PersistenceMgr` 局外存档；**禁止**进战斗时清空。战斗临时状态仅在 `Combat.ResetCombatState()` 清理
7. 场景路径写入 `ScenePaths`；配置改 xlsx 不改生成文件

## 目录速查

`gameplay/` 战斗 · `meta/` 局外 · `framework/` 基础设施
