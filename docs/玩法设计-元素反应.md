# 元素反应 (Elemental Resonance)

> 基于 Hope 项目当前架构与代码现状
> 设计原则：可行性优先，创意其次，好玩是最终目标
> 2026年7月

---

**核心创意**：为武器和技能增加元素属性（火、冰、雷、毒），不同元素之间的组合会产生独特的反应效果。不再是单纯堆伤害，而是要考虑元素搭配策略。

## 为什么选这个

这是目前游戏**最缺的一个维度**——所有伤害都是"物理伤害"，没有任何属性区分。元素系统让 Build 构筑多了一个决策层：选什么元素？怎么搭配技能和装备的元素？而且元素反应的"化学反应"感很强，视觉反馈丰富。

## 元素体系

### 4 种基础元素

| 元素 | 颜色 | 基础效果 | 获得方式 |
|------|------|----------|----------|
| **火** | 🔴 红 | 命中附加燃烧(DOT 3s) | 武器元素词条、传奇威能 |
| **冰** | 🔵 蓝 | 命中附加减速 20%，持续 2s | 同上 |
| **雷** | 🟡 黄 | 命中 15% 概率麻痹 0.5s | 同上 |
| **毒** | 🟢 绿 | 命中附加中毒(DOT 5s，可叠加 3 层) | 同上 |

### 元素反应表

| 组合 | 反应名称 | 效果 | 视觉 |
|------|---------|------|------|
| **火 + 火** | 烈焰 | 燃烧伤害 x1.5，扩散到附近 1 个敌人 | 火焰溅射 |
| **冰 + 冰** | 冻结 | 减速叠加到 60% 时长 1s | 冰冻碎裂 |
| **雷 + 雷** | 过载 | 麻痹概率提升到 40% | 电流增强 |
| **毒 + 毒** | 剧毒 | 中毒层数上限提升到 5 层 | 毒雾加深 |
| **火 + 冰** | 蒸发 | 移除燃烧和减速，造成 2 倍瞬时伤害 | 白色蒸汽爆炸 |
| **火 + 雷** | 爆炸 | 范围爆炸，半径 60px，造成 1.5 倍伤害 | 橙红色爆炸圈 |
| **火 + 毒** | 焚毒 | 燃烧和中毒合并为"焚毒"DOT，每秒伤害 x2 | 紫红色火焰 |
| **冰 + 雷** | 超导 | 范围减速 50% + 麻痹 1s | 蓝白色电网 |
| **冰 + 毒** | 凝滞 | 冻结 DOT 计时，冻结结束后造成累积伤害 | 绿色冰晶 |
| **雷 + 毒** | 腐蚀 | 降低目标护甲 20%，持续 3s | 黄绿色侵蚀 |
| **火+冰+雷** | 毁灭 | 大范围爆炸，清除全场普通敌人 | 全屏闪光 |

### 元素配置表

新增 `tools/config/element.xlsx`：

| 字段 | 说明 |
|------|------|
| element_id | 主键 1=火 2=冰 3=雷 4=毒 |
| name | 元素名称 |
| color | 颜色 hex |
| dot_type | DOT 类型(burn/freeze/shock/poison) |
| dot_damage | 每秒 DOT 伤害百分比 |
| dot_duration | 基础持续秒数 |
| status_effect | 状态效果(slow/paralyze/none) |
| status_chance | 基础触发概率 |

反应表 `tools/config/element_reaction.xlsx`：

| 字段 | 说明 |
|------|------|
| reaction_id | 主键 |
| element_a | 元素 A |
| element_b | 元素 B |
| reaction_name | 反应名称 |
| effect_type | 效果类型(damage/explode/armor_reduce/freeze) |
| effect_value | 效果数值 |
| radius | 范围(0=单体) |
| visual_effect | 视觉资源 |

## 实现方案

### 代码架构

```
scripts/gameplay/elements/
├── ElementType.cs              ← 元素枚举
├── ElementalDamage.cs          ← 元素伤害结构体
├── ElementManager.cs           ← 元素管理器（Autoload）
├── ElementReactionResolver.cs  ← 反应解析引擎
├── StatusEffectComponent.cs    ← 状态效果组件（附加到敌人）
└── ElementalWeaponSlot.cs      ← 扩展武器槽（替代原 WeaponSlot）
```

### 核心逻辑

```csharp
// ElementalDamage — 每个伤害实例携带的元素信息
public struct ElementalDamage
{
    public float BaseDamage;
    public List<ElementType> Elements;  // 可同时携带多种元素
    public float ProcChance;           // 触发概率
}

// ElementManager
public partial class ElementManager : Node
{
    // 查询两种元素组合的反应
    public ElementReaction? GetReaction(ElementType a, ElementType b)
    {
        // 查配置表
    }

    // 处理攻击命中时的元素效果
    public void ProcessElementalHit(Enemy enemy, ElementalDamage damage)
    {
        var activeElements = new List<ElementType>();

        // 1. 应用元素状态效果
        foreach (var element in damage.Elements)
        {
            ApplyStatusEffect(enemy, element, damage.ProcChance);
            activeElements.Add(element);
        }

        // 2. 检查敌人身上已有的元素状态
        var existingElements = GetStatusComponent(enemy).ActiveElements;

        // 3. 检测元素反应
        foreach (var existing in existingElements)
        {
            foreach (var newElem in activeElements)
            {
                var reaction = GetReaction(existing, newElem);
                if (reaction != null)
                {
                    TriggerReaction(enemy, reaction.Value);
                    // 消耗已有元素状态
                    GetStatusComponent(enemy).RemoveElement(existing);
                    return; // 每帧只触发一次反应
                }
            }
        }

        // 4. 记录新元素到敌人状态
        foreach (var element in activeElements)
            GetStatusComponent(enemy).AddElement(element);
    }
}
```

### WeaponSlot 改动

`WeaponSlot.cs` 增加元素属性：

```csharp
// 新增
[Export] public ElementType ElementType { get; set; }

// 攻击命中时
private void OnProjectileHit(Enemy enemy)
{
    var damage = new ElementalDamage
    {
        BaseDamage = _currentDamage,
        Elements = new List<ElementType> { ElementType },
        ProcChance = 0.2f  // 20% 基础触发概率，受装备加成
    };
    ElementManager.Instance.ProcessElementalHit(enemy, damage);
}
```

### 敌人增强

`Enemy.cs` 增加 `StatusEffectComponent`：

```csharp
public partial class StatusEffectComponent : Node
{
    private Dictionary<ElementType, float> _activeTimers = new();
    private Dictionary<ElementType, int> _stacks = new();

    public IReadOnlyCollection<ElementType> ActiveElements => _activeTimers.Keys;

    public void AddElement(ElementType type, float duration = 2.0f)
    {
        // 叠加或刷新
    }

    public void RemoveElement(ElementType type) { }

    public override void _Process(double delta)
    {
        // DOT 伤害计时
        // 状态效果应用（减速/麻痹）
        // 超时清除
    }
}
```

### 改动范围评估

| 模块 | 工作量 | 备注 |
|------|--------|------|
| 配置表(element.xlsx + reaction.xlsx) | 小 | 约 30 行数据 |
| ElementManager | 中(200 行) | 核心逻辑 |
| StatusEffectComponent | 中(150 行) | 敌人挂载 |
| WeaponSlot 改动 | 小 | 加属性引用 |
| Projectile | 小 | 携带元素数据 |
| 视觉反馈 | 中 | 粒子/颜色叠加 |

**总工作量：约 4-5 天**

---

> **数据来源**：
> - Hope 项目完整代码分析（scripts/ 下 72 个 .cs 文件）
> - 配置数据分析（assets/config/ + tools/config/）
> - 场景结构分析（scenes/）
> - 项目架构文档（docs/ARCHITECTURE.md）
